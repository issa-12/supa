# Force the public npm registry for this project so package-lock.json never gets
# pinned to a private/corporate registry (e.g. Verdaccio), which breaks installs
# in Docker / on other machines that can't authenticate to it.
registry=https://registry.npmjs.org/
